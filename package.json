{
  "name": "society-gym",
  "version": "1.0.0",
  "description": "Society gym management app — monorepo root",
  "private": true,
  "scripts": {
    "predev": "kill-port 4000 5173",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "npm run install:all && echo '\\n✅ Done! Now fill in your .env then run: npm run dev'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1"
  }
}